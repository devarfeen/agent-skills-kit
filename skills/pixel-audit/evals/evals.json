{
  "skill": "pixel-audit",
  "kind": "trigger-eval",
  "how_to_run": "Build a catalog of every installed skill's name + description (this kit's skills plus the externals/companions a workspace normally has), mix all eval queries into one numbered list, and have 3 independent judges route each query using ONLY the catalog — no repo access. Majority vote per query. Pass = trigger queries route here; no-trigger queries route anywhere else ('route' names the expected sibling, diagnostic only). In this repo, tools/trigger-evals/ holds the checked-in harness.",
  "queries": [
    {
      "q": "Pixel-audit the assets list page in ADMIN-WEB against this Figma node",
      "expect": "trigger"
    },
    {
      "q": "This page must match the design pixel-for-pixel before we ship",
      "expect": "trigger"
    },
    {
      "q": "Audit MOBILE-APP order detail against the legacy screens — no Figma exists",
      "expect": "trigger"
    },
    {
      "q": "The user form drifted from the mock — do a strict visual conformance pass on it",
      "expect": "trigger"
    },
    {
      "q": "Compare the rendered billing page to its Figma frame and fix every mismatch",
      "expect": "trigger"
    },
    {
      "q": "Check the dashboard's empty and error states match the design exactly",
      "expect": "trigger"
    },
    {
      "q": "Verify the transfers page against the reference implementation, element by element",
      "expect": "trigger"
    },
    {
      "q": "QA says the layout is off vs design on /assets — run the full audit and prove the fixes",
      "expect": "trigger"
    },
    {
      "q": "Make sure the new settings screen matches Figma, including below the fold",
      "expect": "trigger"
    },
    {
      "q": "Billing header says 'Recieve invoices' — punch-list it",
      "expect": "no-trigger",
      "route": "/polish-batch"
    },
    {
      "q": "Set up the design system for ADMIN-WEB from Figma",
      "expect": "no-trigger",
      "route": "/design-system"
    },
    {
      "q": "Why does the modal flicker when it opens?",
      "expect": "no-trigger",
      "route": "/diagnosing-bugs"
    },
    {
      "q": "Fold the shipped page's new UI back into the library",
      "expect": "no-trigger",
      "route": "/design-system"
    },
    {
      "q": "Screenshot the login page for the release notes",
      "expect": "no-trigger",
      "route": "none"
    },
    {
      "q": "Trace how the preview route renders components",
      "expect": "no-trigger",
      "route": "/feature-discovery"
    },
    {
      "q": "Build the integration contract for PRD-142",
      "expect": "no-trigger",
      "route": "/integration-contract"
    },
    {
      "q": "Add a failing test for the badge component's disabled state",
      "expect": "no-trigger",
      "route": "/tdd-loop"
    },
    {
      "q": "Review this PR's CSS changes",
      "expect": "no-trigger",
      "route": "/code-review"
    },
    {
      "q": "Before we ship, walk the invoice page against Figma node 12:340 and fix anything that's off",
      "expect": "trigger"
    },
    {
      "q": "Our rebuilt reports screen should be indistinguishable from the old app's — check it element by element and fix the gaps",
      "expect": "trigger"
    },
    {
      "q": "Make the landing page feel more polished and modern",
      "expect": "no-trigger",
      "route": "impeccable"
    },
    {
      "q": "The save button on settings is misaligned — fix that one nit",
      "expect": "no-trigger",
      "route": "/polish-batch"
    },
    {
      "q": "Audit the design consistency of every page in the app",
      "expect": "no-trigger",
      "route": "impeccable"
    }
  ],
  "last_run": {
    "date": "2026-08-21",
    "method": "3 independent catalog-routing judges over the 332-query mixed set (all sixteen eval files interleaved, deterministic shuffle), majority vote per query; judges saw only the catalog — no repo access, no SKILL.md",
    "result": "23/23 pass — unanimous pick agreement on every query",
    "judge_model": "claude-opus-5 (3 independent catalog-only runs)",
    "catalog": "tools/trigger-evals/build-catalog.sh (kit descriptions read live 2026-08-21) + catalog-externals.md snapshot dated 2026-08-06 (mattpocock/skills v1.2.0)"
  }
}
